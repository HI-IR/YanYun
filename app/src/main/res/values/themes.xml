<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.YanYun" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.YanYun" parent="Base.Theme.YanYun" />

    <style name="circleImageStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="selectText">
        <item name="android:textSize">12sp</item>
        <item name="android:drawablePadding">2dp</item>
    </style>

    <!-- 在res/values/styles.xml中修改主题 -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- 确保底部导航栏不显示选中背景 -->
        <item name="bottomNavigationStyle">@style/CustomBottomNavigationView</item>
    </style>

    <!-- 创建一个自定义样式 -->
    <style name="CustomBottomNavigationView">
        <item name="itemBackground">@android:color/transparent</item> <!-- 选项背景透明 -->
    </style>

</resources>